import React from 'react';

const MenuItem = (props) => {
    return (
        <div>
            <ol>
                {/*{props.ind + 1}) {props.item}*/}
                {props.ind + 1}) {props.item}
            </ol>
        </div>
    );
};

export default MenuItem;
